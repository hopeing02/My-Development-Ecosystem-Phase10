version: 1
task:
  id: MOBILE-AI-YAML-TEST-20260717-V3
  type: feature
  title: Verify end-to-end mobile YAML automatic push
  description: Generate, apply, validate, commit, and push a deterministic artifact from GitHub YAML.
  workflow: feature
  source: github-mobile-test
  repository:
    branch: feature/mobile-ai-yaml-test-20260717-v3
    base_branch: test/mobile-yaml-trigger-20260717-v3
  execution:
    auto_apply: true
    run_tests: true
    auto_save: true
    auto_push: true
  inputs:
    ai:
      provider: mock
      artifacts:
        - path: generated/mobile-yaml-ai-test-v3.txt
          content: End-to-end mobile YAML AI generation and automatic push verified.
  acceptance:
    - Generated file is committed and pushed to the task branch.
